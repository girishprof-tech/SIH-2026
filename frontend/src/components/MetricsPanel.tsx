import { Activity, Gauge, Timer, Zap } from 'lucide-react'
import { Area, AreaChart, ResponsiveContainer, Tooltip, XAxis, YAxis } from 'recharts'
import type { Metrics } from '../types'
export function MetricsPanel({ metrics, history }: { metrics: Metrics | null; history: Array<{ tick: number; process: number; planner: number; conflicts: number; replans: number }> }) {
  return <section className="metrics panel"><div className="panel-heading"><div><span className="eyebrow">TELEMETRY STREAM</span><h2>PERFORMANCE SIGNALS</h2></div><Activity size={15} /></div><div className="metric-cards"><div><Timer size={14} /><span>TICK PROCESS</span><strong>{metrics?.last_tick_processing_ms?.toFixed(1) ?? '--'}<small> ms</small></strong></div><div><Zap size={14} /><span>PLANNER LATENCY</span><strong>{metrics?.planner_latency_ms?.toFixed(1) ?? '--'}<small> ms</small></strong></div><div><Gauge size={14} /><span>ACTIVE CONFLICTS</span><strong>{metrics?.active_conflicts ?? '--'}</strong></div><div><Activity size={14} /><span>REPLANS</span><strong>{metrics?.replans ?? '--'}</strong></div></div><div className="chart"><ResponsiveContainer width="100%" height="100%"><AreaChart data={history}><defs><linearGradient id="process" x1="0" y1="0" x2="0" y2="1"><stop offset="0%" stopColor="#57a9ff" stopOpacity={.35} /><stop offset="100%" stopColor="#57a9ff" stopOpacity={0} /></linearGradient></defs><XAxis dataKey="tick" hide /><YAxis hide domain={[0, 'auto']} /><Tooltip contentStyle={{ background: '#17242b', border: '1px solid #2f4a53', fontSize: 11 }} labelFormatter={value => `TICK ${value}`} /><Area type="monotone" dataKey="process" stroke="#57a9ff" fill="url(#process)" strokeWidth={2} /></AreaChart></ResponsiveContainer></div></section>
}